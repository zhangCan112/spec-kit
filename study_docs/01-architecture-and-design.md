# 01 - 架构设计与目录结构

## 🏗️ 项目整体架构

Spec Kit 采用分层架构设计，各层职责清晰，通过标准化的接口进行交互：

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户交互层                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────────────────┐  │
│  │ CLI 命令行   │  │  AI 代理    │  │      IDE 集成           │  │
│  │ (specify)   │  │ (Claude等)  │  │   (VS Code 插件等)      │  │
│  └─────────────┘  └─────────────┘  └──────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        命令执行层                                    │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │            命令模板系统 (templates/commands/*.md)         │   │
│  │  - specify.md  - plan.md  - tasks.md  - implement.md        │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        自动化脚本层                                  │
│  ┌───────────────────┐              ┌─────────────────────────┐    │
│  │  Bash 脚本        │              │  PowerShell 脚本         │    │
│  │  scripts/bash/    │              │  scripts/powershell/   │    │
│  │  - create-new-   │              │  - create-new-feature   │    │
│  │    feature.sh     │              │    .ps1                 │    │
│  │  - setup-plan.sh  │              │  - setup-plan.ps1        │    │
│  │  - update-agent-  │              │  - update-agent-context  │    │
│  │    context.sh     │              │    .ps1                  │    │
│  └───────────────────┘              └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        文档管理层                                    │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              规范文档结构 (specs/XXX-feature/)              │   │
│  │  - spec.md         - plan.md       - tasks.md                │   │
│  │  - research.md     - data-model.md - contracts/              │   │
│  │  - quickstart.md   - checklists/                            │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        模板资源层                                    │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              文档模板 (templates/)                          │   │
│  │  - spec-template.md      - plan-template.md               │   │
│  │  - tasks-template.md    - constitution-template.md        │   │
│  │  - agent-file-template.md  - checklist-template.md        │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Git 与版本控制层                               │
│  ┌───────────────────┐  ┌───────────────────┐  ┌─────────────────┐│
│  │   本地 Git 仓库   │  │   远程仓库         │  │   分支管理       ││
│  │   (git init)      │  │   (origin/main)   │  │   (feature/**)   ││
│  └───────────────────┘  └───────────────────┘  └─────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        外部服务层                                    │
│  ┌───────────────────┐              ┌─────────────────────────┐    │
│  │   GitHub API      │              │   AI 服务提供商         │    │
│  │   模板下载        │              │   (Anthropic, Google等)  │    │
│  └───────────────────┘              └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📁 目录结构深度解析

### 根目录结构

```
spec-kit/
├── .gitattributes              # Git 属性配置
├── .gitignore                 # Git 忽略规则
├── .markdownlint-cli2.jsonc   # Markdown Lint 配置
├── AGENTS.md                  # AI 代理集成指南（重要！）
├── CHANGELOG.md               # 变更日志
├── CODE_OF_CONDUCT.md         # 行为准则
├── CONTRIBUTING.md            # 贡献指南
├── LICENSE                    # MIT 许可证
├── pyproject.toml             # Python 项目配置
├── README.md                  # 项目说明文档
├── SECURITY.md                # 安全策略
├── spec-driven.md             # SDD 方法论文档（核心！）
├── SUPPORT.md                 # 支持信息
├── docs/                      # 官方文档
├── media/                     # 媒体资源（图片、GIF）
├── scripts/                   # 自动化脚本
├── src/                       # 源代码目录
│   └── specify_cli/          # Specify CLI 包
│       └── __init__.py      # 主实现文件（单文件架构）
└── templates/                 # 模板目录
```

### src/specify_cli/ 目录

```
src/specify_cli/
└── __init__.py               # 核心 CLI 实现（约 1000+ 行）
    ├── AGENT_CONFIG          # AI 代理配置字典
    ├── StepTracker          # 进度追踪类
    ├── BannerGroup          # 自定义 Typer 组
    ├── 主函数和命令          # init(), check(), version()
    ├── 工具函数            # download_template_from_github()
    │                      # merge_json_files()
    │                      # is_git_repo()
    │                      # init_git_repo()
    ├── UI 组件             # select_with_arrows()
    │                      # show_banner()
    └── 辅助函数            # _github_token()
                          # _format_rate_limit_error()
```

### templates/ 目录

```
templates/
├── commands/                 # AI 代理命令模板
│   ├── analyze.md           # 交叉一致性分析命令
│   ├── checklist.md         # 质量检查清单生成命令
│   ├── clarify.md           # 需求澄清命令
│   ├── constitution.md      # 宪章管理命令
│   ├── implement.md        # 实现执行命令
│   ├── plan.md             # 技术计划命令
│   ├── specify.md          # 规范创建命令（核心！）
│   ├── tasks.md            # 任务分解命令
│   └── taskstoissues.md     # 任务转 Issues 命令
├── spec-template.md         # 功能规范模板
├── plan-template.md         # 实现计划模板
├── tasks-template.md       # 任务模板
├── constitution-template.md # 宪章模板
├── checklist-template.md   # 检查清单模板
├── agent-file-template.md  # AI 代理文件模板
└── vscode-settings.json    # VS Code 设置模板
```

### scripts/ 目录

```
scripts/
├── bash/                    # Bash 脚本（Linux/macOS）
│   ├── check-prerequisites.sh      # 前置条件检查
│   ├── common.sh                   # 公共函数
│   ├── create-new-feature.sh      # 创建新特性（核心！）
│   ├── setup-plan.sh              # 设置计划环境
│   └── update-agent-context.sh    # 更新 AI 代理上下文
└── powershell/              # PowerShell 脚本（Windows）
    ├── check-prerequisites.ps1
    ├── common.ps1
    ├── create-new-feature.ps1
    ├── setup-plan.ps1
    └── update-agent-context.ps1
```

### 典型项目结构（specify init 后）

```
your-project/
├── .git/                      # Git 仓库
├── .specify/                  # Specify 配置目录
│   ├── memory/               # 内存和上下文
│   │   └── constitution.md   # 项目宪章
│   ├── scripts/              # 项目脚本
│   │   ├── check-prerequisites.sh
│   │   ├── common.sh
│   │   ├── create-new-feature.sh
│   │   └── setup-plan.sh
│   └── templates/            # 项目模板
│       ├── plan-template.md
│       ├── spec-template.md
│       └── tasks-template.md
├── specs/                    # 规范文档目录
│   └── 001-add-user-auth/    # 特性分支目录
│       ├── spec.md          # 功能规范
│       ├── plan.md          # 实现计划
│       ├── tasks.md         # 任务列表
│       ├── research.md      # 研究文档
│       ├── data-model.md    # 数据模型
│       ├── quickstart.md    # 快速开始指南
│       ├── contracts/       # API 契约
│       │   ├── api-spec.json
│       │   └── signalr-spec.md
│       └── checklists/      # 检查清单
│           └── requirements.md
├── .claude/                  # Claude 配置（如果使用 Claude）
│   └── commands/
│       └── specify-rules.md  # Claude 上下文文件
├── .gitignore
└── README.md
```

---

## 🔗 组件交互关系

### 1. CLI 初始化流程

```
用户执行: specify init my-project --ai claude
    │
    ▼
┌─────────────────────────────────┐
│ StepTracker 初始化               │
│ - 显示横幅                      │
│ - 验证前置条件                  │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 交互式选择                       │
│ - 选择 AI 代理                  │
│ - 选择脚本类型                  │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 下载模板                         │
│ - 调用 GitHub API               │
│ - 下载 agent 特定的 ZIP 包        │
│ - 提取到项目目录                │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 项目初始化                       │
│ - 创建目录结构                  │
│ - 合并 VS Code 设置              │
│ - 设置脚本权限                  │
│ - 初始化 Git 仓库               │
└─────────────────────────────────┘
    │
    ▼
完成！项目准备就绪
```

### 2. AI 代理工作流程

```
用户: /speckit.specify Add user authentication
    │
    ▼
┌─────────────────────────────────┐
│ AI 代理加载命令模板             │
│ templates/commands/specify.md   │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 执行前置脚本                    │
│ scripts/bash/create-new-feature.sh│
│ - 计算特性号                   │
│ - 生成分支名                    │
│ - 创建 Git 分支                 │
│ - 初始化规范文件                │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ AI 按照模板生成内容            │
│ - 分析用户输入                 │
│ - 填充规范模板                 │
│ - 创建规范文档                  │
└─────────────────────────────────┘
    │
    ▼
完成！规范已创建
```

### 3. 计划生成流程

```
用户: /speckit.plan Use OAuth2 with JWT
    │
    ▼
┌─────────────────────────────────┐
│ AI 代理加载命令模板             │
│ templates/commands/plan.md      │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 执行前置脚本                    │
│ scripts/bash/setup-plan.sh      │
│ - 定位规范文件                  │
│ - 准备计划环境                  │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Phase 0: 研究阶段               │
│ - 识别技术未知数                │
│ - 生成研究文档                  │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Phase 1: 设计阶段               │
│ - 生成数据模型                  │
│ - 创建 API 契约                 │
│ - 生成快速开始指南              │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 更新 AI 代理上下文              │
│ scripts/bash/update-agent-context.sh│
│ - 添加新技术信息                │
└─────────────────────────────────┘
    │
    ▼
完成！计划已创建
```

---

## 🎯 关键设计决策

### 1. 单文件架构

`src/specify_cli/__init__.py` 将所有 CLI 逻辑放在一个文件中：

**优点**：
- 简化部署（单一 Python 包）
- 易于理解和维护
- 减少导入复杂性

**缺点**：
- 文件较大（约 1000+ 行）
- 需要良好的代码组织

### 2. 跨平台脚本支持

同时提供 Bash 和 PowerShell 脚本：

**优点**：
- 完整的跨平台支持
- 每个平台使用原生脚本
- 最大化兼容性

**实现**：
- 共享逻辑在 `common.sh/ps1`
- 特定平台逻辑独立实现
- 统一的接口和输出格式

### 3. 模板驱动架构

所有 AI 交互通过 Markdown 模板：

**优点**：
- 可读性强（自然语言）
- 易于修改和扩展
- 版本控制友好
- AI 友好

**特点**：
- Front Matter 元数据（YAML）
- 结构化内容
- 明确的占位符

### 4. 分支编号系统

自动化的特性分支命名：`###-feature-name`

**规则**：
- 3 位数字前缀（001, 002, ...）
- 短描述符（2-4 个词）
- 自动递增
- 跨分支和文档目录同步

### 5. JSON 配置驱动

`AGENT_CONFIG` 字典作为 AI 代理配置的单真源：

**优点**：
- 集中管理
- 类型安全
- 易于扩展
- 自动工具检查

---

## 🔌 扩展点

### 1. 添加新的 AI 代理

修改 `src/specify_cli/__init__.py` 中的 `AGENT_CONFIG`：

```python
AGENT_CONFIG = {
    "new-agent": {
        "name": "New Agent Display Name",
        "folder": ".newagent/",
        "install_url": "https://example.com/install",
        "requires_cli": True/False,
    },
}
```

### 2. 添加新的命令模板

在 `templates/commands/` 创建新的 `.md` 文件：

```markdown
---
description: Command description
scripts:
  sh: scripts/bash/my-command.sh
  ps: scripts/powershell/my-command.ps1
---

Command content...
```

### 3. 扩展项目模板

修改 `templates/` 下的模板文件，支持自定义的工作流和文档结构。

---

## 📊 数据流图

```
┌──────────────┐
│   用户输入   │
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌─────────────────┐
│  CLI 命令    │────▶│  脚本执行器      │
└──────┬───────┘     │ (Bash/PS)       │
       │            └────────┬─────────┘
       ▼                     │
┌──────────────┐            ▼
│  模板加载    │     ┌──────────────┐
│  (Markdown)  │────▶│  文件系统操作  │
└──────┬───────┘     │ (创建/修改)   │
       │            └──────────────┘
       ▼
┌──────────────┐
│  AI 处理     │
│  (生成内容)  │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  输出文档    │
│  (specs/)    │
└──────────────┘
```

---

## 🎓 架构学习要点

1. **分层清晰**：每层有明确的职责，通过标准接口通信
2. **模板驱动**：所有 AI 交互通过可读的 Markdown 模板
3. **跨平台**：Bash 和 PowerShell 双脚本支持
4. **可扩展**：通过配置字典和模板系统易于扩展
5. **Git 集成**：深度集成 Git 工作流，分支管理自动化

下一节将深入 CLI 的具体实现细节。